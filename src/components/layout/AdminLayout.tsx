import { Outlet } from "react-router";
const AmdinLayout = () => {
    return (
        <div>
            <h1>Admin Layout</h1>
            <Outlet />
        </div>
    );
}
export default AmdinLayout;